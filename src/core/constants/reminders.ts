/** Maximum number of pending reminders an agent may have at once. */
export const MAX_REMINDERS_PER_AGENT = 200;

/** Maximum length, in characters, of a single reminder's text. */
export const REMINDER_TEXT_MAX_LENGTH = 500;
