{
  "name": "@jazz/runtime",
  "version": "0.15.6",
  "private": true,
  "description": "Composition root \u2014 wires @jazz/core, @jazz/adapters, and @jazz/cli into the running Jazz CLI. Compiled into the standalone binary.",
  "type": "module",
  "exports": {
    "./*": [
      "./src/*.ts",
      "./src/*/index.ts"
    ]
  },
  "dependencies": {
    "@jazz/adapters": "workspace:*",
    "@jazz/cli": "workspace:*",
    "@jazz/core": "workspace:*"
  }
}
