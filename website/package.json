{
  "name": "jazz-website",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "tokens": "bun run scripts/generate-tokens.ts",
    "build": "bun run tokens && astro build && pagefind --site dist",
    "check-links": "bun run scripts/check-links.ts",
    "preview": "astro preview"
  },
  "dependencies": {
    "@astrojs/markdown-remark": "^7.2.4",
    "astro": "^7.2.4",
    "mermaid": "^11.17.0"
  },
  "devDependencies": {
    "pagefind": "^1.5.2"
  }
}
